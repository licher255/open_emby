{
  "name": "open_emby",
  "version": "0.1.0",
  "description": "AI embroidery digitizing studio — turn any picture into embroidery",
  "main": "out/main/index.js",
  "license": "SEE LICENSE IN LICENSE",
  "scripts": {
    "dev": "electron-vite dev",
    "build": "electron-vite build",
    "start": "electron-vite preview",
    "dist": "electron-vite build && electron-builder",
    "typecheck": "tsc --noEmit -p tsconfig.node.json && tsc --noEmit -p tsconfig.web.json",
    "sidecar": "pnpm --dir sidecar run dev",
    "core:build": "pnpm --dir crates/emby-core run build",
    "setup": "pnpm install && pnpm --dir sidecar install && pnpm --dir crates/emby-core install && pnpm run core:build"
  },
  "devDependencies": {
    "@types/node": "^26.5.1",
    "@types/react": "^18.3.18",
    "@types/react-dom": "^18.3.5",
    "@vitejs/plugin-react": "^4.3.4",
    "electron": "^33.4.11",
    "electron-builder": "^25.1.8",
    "electron-vite": "^2.3.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "typescript": "^5.7.3",
    "vite": "^5.4.14"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "zustand": "^5.0.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "electron",
      "esbuild"
    ]
  }
}
